/**
 * Interface que representa as informações de um filme
 * Usada para padronizar os dados de filmes em toda a aplicação
 */
export interface MovieInfo {
    title: string;  // Título do filme
    plot: string;   // Sinopse/plot do filme
}